import React, { FC } from 'react'

interface MessageClientProps {
  
}

const MessageClient: FC<MessageClientProps> = ({  }) => {
  return (
    <div>
     MessageClient
    </div>
  )
}

export default MessageClient;